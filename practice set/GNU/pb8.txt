f(x) = x**a * b**x

a = 1
b = 1

fit f(x) "data3.txt" using 1:2 via a,b

set terminal pdfcairo
set output "fitt.pdf"

set title "data fitting"
set xlabel "X"
set ylabel "Y"

plot "data3.txt" using 1:2 with points title "data" pt 7 ps 1.0 ,\
f(x) title "fit" lw 2 lc 2
set output

